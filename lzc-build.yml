manifest: ./lzc-manifest.yml
pkgout: ./
icon: ./icon.png

compose_override:
  services:
    forgejo-server:
      volumes:
        - /etc/timezone:/etc/timezone:ro
        - /etc/localtime:/etc/localtime:ro